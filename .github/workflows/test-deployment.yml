name: Test Deployment and API

on:
  workflow_run:
    workflows: ["Deploy Backend to Azure", "Deploy to GitHub Pages"]
    types: [completed]
  workflow_dispatch:

jobs:  test-deployment:
on:
  workflow_run:
    workflows: ["Deploy Backend to Azure", "Deploy to GitHub Pages"]
    types: [completed]
  workflow_dispatch:

jobs: